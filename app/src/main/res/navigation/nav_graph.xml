<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment android:id="@+id/homeFragment"
        android:name="com.tes.jk.HomeFragment"
        android:label="@string/home"
        tools:layout="@layout/fragment_home" />

    <!-- Ganti DashboardFragment menjadi CompilerFragment -->
    <fragment android:id="@+id/compilerFragment"
        android:name="com.tes.jk.CompilerFragment"
        android:label="@string/compiler"
        tools:layout="@layout/fragment_compiler" />

    <fragment android:id="@+id/profileFragment"
        android:name="com.tes.jk.ProfileFragment"
        android:label="@string/profile"
        tools:layout="@layout/fragment_profile" />

</navigation>